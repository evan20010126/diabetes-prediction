from setuptools import setup, find_packages

setup(
    name='diabetes-prediction',
    version='0.1.0',
    author='Evan',
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.7',
)
